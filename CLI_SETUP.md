# 🚀 CLI для HzStack - Готово к публикации!

## 📦 Что уже создано

### CLI пакет (`cli/`):
- ✅ **package.json** - настроен для публикации в npm
- ✅ **src/index.ts** - основной CLI код с полной логикой
- ✅ **tsconfig.json** - TypeScript конфигурация
- ✅ **README.md** - документация CLI
- ✅ **.gitignore** - исключение ненужных файлов

### Основной проект:
- ✅ **README.md** - обновлен с информацией о CLI
- ✅ **.env.example** - пример переменных окружения
- ✅ **DEPLOY_CLI.md** - инструкции по публикации

## 🎯 Что делает CLI

```bash
npx create-hzstack my-app
```

1. **Клонирует** репозиторий `hzpunk/hzstack`
2. **Очищает** от `.git`, `node_modules`, lock-файлов
3. **Обновляет** `package.json` с новым именем проекта
4. **Устанавливает** зависимости (опционально)
5. **Показывает** инструкции по настройке

## 📋 Следующие шаги для публикации

### 1. Залить основной проект на GitHub

```bash
git remote add origin https://github.com/hzpunk/hzstack.git
git push -u origin master
```

### 2. Настроить CLI

```bash
cd cli
npm install
npm run build
```

### 3. Протестировать CLI локально

```bash
npm link
create-hzstack test-project
```

### 4. Опубликовать в npm

```bash
npm login
npm publish
```

## 🔧 Технические детали CLI

### Зависимости:
- `commander` - парсинг аргументов
- `chalk` - цветной вывод
- `ora` - спиннеры загрузки
- `fs-extra` - работа с файлами
- `inquirer` - интерактивные вопросы
- `cross-spawn` - запуск процессов

### Опции:
- `[project-name]` - имя проекта
- `--no-install` - пропустить установку зависимостей
- `--use-yarn` - использовать yarn вместо npm
- `-t, --template` - выбор шаблона (заглушка)

### Безопасность:
- ✅ Валидация имени проекта
- ✅ Проверка существования директории
- ✅ Очистка чувствительных данных
- ✅ Обработка ошибок

## 🎨 Пример использования

```bash
# Базовое использование
npx create-hzstack my-app

# С опциями
npx create-hzstack my-app --use-yarn
npx create-hzstack my-app --no-install

# Интерактивный режим
npx create-hzstack
```

## 📁 Структура после создания

```
my-app/
├── src/app/              # Next.js App Router
├── src/components/       # React компоненты
├── src/lib/             # Утилиты
├── src/store/           # Zustand store
├── prisma/              # База данных
├── public/              # Статические файлы
├── .env.example         # Пример конфига
├── package.json         # Зависимости
└── README.md            # Документация
```

## 🚀 После создания проекта

```bash
cd my-app
cp .env.example .env
# Настроить DATABASE_URL и JWT_SECRET
npx prisma migrate dev
npm run dev
```

## 🎯 Результат

Пользователь сможет одной командой создать полный проект с:
- 🔐 Аутентификацией
- 🛡️ Админ-панелью
- 📊 Аудитом
- 🎨 Современным UI
- 📱 Адаптивным дизайном
- 🔔 Уведомлениями
- 🖼️ Загрузкой аватаров

---

**CLI готов к публикации!** 🎉

Осталось только:
1. Залить основной проект на GitHub
2. Опубликовать CLI пакет в npm
3. Пользоваться! 🚀
